#!groovy

@Library(["github", "notifications"]) _

pipeline {
    agent {
        node {
            label 'default||master'
        }
    }
    options {
        buildDiscarder(logRotator(numToKeepStr: '10'))
    }
    parameters {
        string(name: 'DOCKER_TAG', defaultValue: 'develop', description: 'Docker tag\'s name')
    }
    stages {
        stage('Create GitHub Release') {
            steps {
                script {
                    github.authenticate token: "8d9b9e9ed512efb4f486482d6776c448f22ebda5"
                    github.release tag: "v4", branch: "develop", description: "v4", draft: true, prerelease: true
                }
            }
        }
    }
    post {
        always {
            deleteDir()
        }
    }
}
