pipeline {
    agent {
        node {
            label 'docker'
        }
    }
    options {
        buildDiscarder(logRotator(numToKeepStr: '10'))
    }
    stages {
        stage('Build image docker') {
            steps {
                script {
                    docker.withRegistry("http://docker-vsct.pkg.cloud.socrate.vsct.fr") {
                        sh "docker login --username admgit --password '7meJ%G\$2' --email deploy@build.fr docker-vsct.pkg.cloud.socrate.vsct.fr"
                        sh "docker build --build-arg http_proxy=http://proxy-hpr:80 --build-arg https_proxy=https://proxy-hpr:80 ./docker -t hesperides/hesperides-spring:springboot"
                        dockerImage = docker.image("hesperides/hesperides-spring:springboot")
                        dockerImage.push()
                    }
                }
            }
        }
    }
}
